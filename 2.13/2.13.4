speed(0)
color_choice = input("What color should the circle be?")

penup()
backward(200)
pendown()

def circle_boom():
    for i in range(1):
        color(color_choice)
        begin_fill()
        circle(20)
        end_fill()
        penup()
        forward(40)
        pendown()
for i in range(8):
    circle_boom()
    color_choice = input("what color?")
