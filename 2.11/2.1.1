speed()
def dart():
    radius = 25
    for i in range(4):
        pendown()
        circle(radius)
        radius = radius + 25
        penup()
        right(90)
        forward(25)
        left(90)
    
dart()
