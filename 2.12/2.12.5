speed()
square_length = int(input("How big should the squares be? "))

def draw_square():
    for i in range(4):
        pendown()
        right(90)
        forward(square_length)
        penup()
penup()
setposition(200, 200)
draw_square()

setposition(200, -200)
right(90)
draw_square()

setposition(-200, 200)
right(90)
right(90)
draw_square()

setposition(-200, -200)
right(90)
right(90)
right(90)
draw_square()
